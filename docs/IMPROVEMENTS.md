# 개선 대장 (jd-search)

발견된 것은 **고쳤든 아니든 전부 여기 남긴다.** 고친 것은 지우지 않고 상태만 바꾼다 —
같은 실수가 다시 들어올 때 "왜 이렇게 돼 있는가"의 근거가 이 목록이기 때문이다.

상태: `✅ 완료` / `🔧 진행` / `📋 대기` / `📌 결정필요`
출처: `자체` (구현·실행 중 발견) / `codex` (외부 리뷰 2026-08-10) / `fable` (판정) / `사용자` (배포 후 제보)

---

## P0 — 사용자가 틀린 정보를 보게 되는 것

| # | 상태 | 출처 | 위치 | 내용 |
|---|---|---|---|---|
| 1 | ✅ | 자체 | `lib/text.mjs` | `(주)골프존`의 `주`를 별칭으로 뽑아 `주 주식회사`로 조회 → 골프존·델레오코리아에 **같은 법인번호**가 붙음. 법인 형태 표기는 별칭이 아니다 |
| 2 | ✅ | 자체 | `lib/match.mjs` | 부분일치를 지역으로 구제해 오매칭 5건. 부분일치는 **어떤 보조 근거로도 채택 금지** |
| 3 | ✅ | codex | `lib/match.mjs:30` | 법인등록번호를 아는데 **불일치하는 후보를 상호 완전일치만으로 채택**. 강한 근거를 약한 근거가 덮어씀 → `crno-mismatch`로 즉시 종료 |

## P1 — 판정이 틀리거나 데이터가 조용히 사라지는 것

| # | 상태 | 출처 | 위치 | 내용 |
|---|---|---|---|---|
| 4 | ✅ | 자체 | `lib/dart_table.mjs` | 회계연도 상한이 제출연도라 리스 만기(미래 12/31)가 잡힘 → **제출일 기준 지나간 12/31**까지만 |
| 5 | ✅ | codex | `lib/dart.mjs` | 보고서 종류를 날짜보다 먼저 봐서 **2023년 사업보고서가 2026년 감사보고서를 이김** → 최신 연도 우선, 종류는 같은 해 안에서만 |
| 6 | ✅ | 자체 | `lib/grade.mjs` | "적자 + 자본 완충 1~3배"가 어느 분기에도 안 걸려 `u`로 빠짐 → `w` |
| 7 | ✅ | 자체 | `lib/grade.mjs` | 자본잠식 + 영업흑자가 `r`(위험) → `w`까지만 |
| 8 | ✅ | codex | `lib/grade.mjs` | **영업이익 정확히 0 / 자본총계 정확히 0**이 어느 분기에도 안 걸려 `u` → 손익분기는 `o`, 자본 ≤ 0은 `r` |
| 9 | ✅ | codex | `lib/grade.mjs` | 두 번째로 최신인 자료를 무조건 "직전연도"로 취급 → **2025·2022만 있어도 "2년 연속 적자"**. 연속 판정은 `prev.year === latest.year - 1`일 때만 |
| 10 | ✅ | codex | `finance.mjs` | 공공데이터 **별도**와 DART **연결**을 한 시계열에 섞어 추세 판정 → 같은 기준끼리만 비교 |
| 11 | ✅ | 자체 | `finance.mjs` | `--only` 실행이 `finance.json`을 통째로 덮어써 나머지 29곳이 사라짐 → 병합 |
| 12 | ✅ | codex | `lib/io.mjs:37` | **손상된 JSON을 파일 부재와 똑같이 삼켜** 빈 값으로 읽은 뒤 덮어씀 → 기존 공고 전멸. `ENOENT`만 fallback, 파싱 오류는 중단 |
| 13 | ✅ | codex | `collect_wanted.mjs` | `--max` 한도에서 잘렸는데 **잘렸다는 사실을 안 남김** → `truncated` 기록 + 리포트 표시 |
| 14 | ✅ | codex | `collect_wanted.mjs` | 키워드 5개 중 2개가 네트워크 실패해도 정상 종료 → 실행 manifest에 키워드별 성공·실패 기록 + 리포트 경고 |
| 15 | ✅ | codex | `collect_wanted.mjs` | 검색조건을 PM→QA로 바꿔도 **옛 PM 공고가 리포트에 계속 남음** → `runId`/`seenAt` 기록, 현재 집합 밖은 `stale` |
| 16 | ✅ | codex | `finance.mjs` | 성공 재무를 **기한 없이** 캐시 → 새 감사보고서가 나와도 옛 등급에 묶임. 회계연도 기준 재검증 |
| 17 | ✅ | 자체 | `collect_wanted.mjs` | JD 원문 파일이 없어도 재수집 안 함 → 공고 마감 시 영구 손실. 없으면 다시 받음 |

## P1 — 개인정보·보안

| # | 상태 | 출처 | 위치 | 내용 |
|---|---|---|---|---|
| 18 | ✅ | codex | `lib/io.mjs` | 프로필·이력서·지원이력이 **755/644**로 생성돼 같은 컴퓨터의 다른 계정이 읽음 → 디렉터리 `0700`, 파일 `0600` |
| 19 | ✅ | codex | `render.mjs` | **지원 상태(면접·탈락)가 공유용 HTML에 그대로** 들어감 → 기본 제외, `--with-status`로만 포함 |
| 20 | ✅ | codex | `lib/pubdata.mjs` | API 오류 메시지(`resultMsg`)를 마스킹 없이 stdout에 출력 → 서버가 키를 반향하면 유출. `mask()` 적용 |

## P2 — 문서와 코드의 불일치 (약속을 지키지 못하는 것)

| # | 상태 | 출처 | 위치 | 내용 |
|---|---|---|---|---|
| 21 | ✅ | codex | `gate.mjs` / README | 문서는 **통근 상한 필터**를 핵심 기능으로 약속하지만 코드는 지역 필터만 한다. `maxCommuteMin`·`hybridFactor`·`maxTransfers`·`precise` 미사용 → 문서를 현재 동작에 맞추고 미구현을 명시 |
| 22 | ✅ | codex | README | `alive` 단계를 동작하는 것처럼 기술 → 미구현 표시, 리포트의 상태는 "수집 당시 상태"로 표기 |
| 23 | ✅ | codex | README | "자택 주소는 좌표로만 보관" 약속과 달리 `profile.yml`에 원문이 남음 → 문서를 사실대로(로컬 프로필에 저장, 리포트에는 미출력) |
| 24 | ✅ | codex | `collect_wanted.mjs` | 문서는 요청 간 최소 1초인데 원티드는 600ms → 1초로 통일 |
| 25 | ✅ | codex | `SKILL.md` / `lib/grade.mjs` | "재무가 없으면 면접 질문을 만든다"는데 `u`는 항상 빈 배열 → 미확인 **사유별** 질문 생성 |
| 26 | ✅ | 자체 | `templates/report.html` | `annualFrom: 0`이 "경력 0년+"로 표시 → "경력 무관" |

## P1 — Fable 판정 중 발견 (2026-08-10)

| # | 상태 | 출처 | 위치 | 내용 |
|---|---|---|---|---|
| 31 | ✅ | fable | `SKILL.md` `boards.md` | 존재하지 않는 `scripts/dart_finance.mjs` 참조, `dart_table.mjs`가 `lib/`로 옮겨졌는데 본문 2곳 미갱신 |
| 32 | ✅ | fable | `finance.mjs` | 공공데이터포털에서 붙은 **ambiguous 플래그를 DART 확정 후에도 안 지움** → 등급이 버젓이 있는데 "동명이인이라 재무를 안 붙였습니다" 노트가 나가고 캐시에 굳음 |
| 33 | ✅ | fable | `lib/region.mjs` | 양방향 `includes` 대조라 사용자가 **`서울 중구`라고 명시해도 부산 중구가 통과**. 중·서·남·동·북·강서구는 전국에 흔해 실사용에서 조용히 새어 들어옴 → 정확 일치 + 모호한 구 이름 경고 |
| 34 | ✅ | fable | `collect_wanted.mjs` `profile.example.yml` | 제목 필터 제외분이 리포트에 안 실림 + 예시 프로필에 `프로덕트 오너`가 없어 **PO 공고가 전량 조용히 사라짐**(실측: 키워드 추가만으로 17건 발견) |
| 35 | ✅ | fable | `collect_wanted.mjs` | 검색 결과에서 빠진 공고는 상세 조회가 안 돼 `status`가 영원히 `active`로 남음 → `check_alive`로 해소 |
| 36 | ✅ | 자체 | `check_alive.mjs` | 재공고 되찾기가 **같은 회사의 무관한 공고까지 끌어옴**(PM 검색에 "Senior Backend Engineer" 유입). 조용한 손실을 막으려다 조용한 잡음을 만듦 → 같은 자리 연결 or 직군 일치만 |

## P1 — 회귀 테스트의 빈틈

| # | 상태 | 출처 | 내용 |
|---|---|---|---|
| 27 | ✅ | codex | 법인번호 **불일치 + 상호 일치**를 검사하지 않음 (위 #3을 못 잡았다) |
| 28 | ✅ | codex | 보고서 **종류와 날짜가 충돌**하는 조합이 없음 (#5를 못 잡았다) |
| 29 | ✅ | codex | 재무 연도가 항상 연속인 입력만 씀 (#9를 못 잡았다) + 경계값 0 없음 (#8) |
| 30 | ✅ | codex | 순수 함수만 검사하고 **단계 간 계약**을 안 봄 → 임시 `JD_SEARCH_HOME`으로 collect→gate→finance→render 통합 테스트 |

---

## P0/P1 — M4 구현 중 실측으로 드러난 것 (2026-08-10)

| # | 상태 | 출처 | 위치 | 내용 |
|---|---|---|---|---|
| 37 | ✅ | 자체 | `lib/saramin.mjs` | **사람인 공고의 본문이 이미지뿐인 경우가 흔하다**(실측 14건 중 3건). 글자 없는 JD 원문 파일을 만들면 "내용이 있는 것처럼 보이는 빈 기록"이 되고, 수집기가 원문 확보로 세어 다시 받지 않는다 → 공고가 내려가면 영구 손실. `imageOnly`/`failed`/`empty` 로 구분하고 이미지 주소를 남긴다 |
| 38 | ✅ | 자체 | `lib/saramin.mjs` | 목록의 마감 표기에 **연도가 없다**(`~ 09/20`). 연도를 추정하면 12월→1월에서 통째로 틀린다 → 목록 값은 `dueLabel` 로만 두고 마감일은 상세 값만 쓴다 |
| 39 | ✅ | 자체 | `lib/saramin.mjs` `lib/region.mjs` `gate.mjs` | 사람인은 **근무지를 여러 곳 적는다**("서울 강남구, 서초구, 대전 서구")는데 목록은 첫 곳만 준다. 첫 곳만 보면 다닐 수 있는 자리를 범위 밖으로 버린다 → `location.all` 에 전부 담고 게이트는 하나라도 들면 통과 |
| 40 | ✅ | 자체 | `lib/saramin.mjs` | 다중 근무지에서 **시·도가 생략된 항목**("서초구")을 그대로 두면 전국의 같은 이름 구가 걸린다 → 앞 항목의 시·도를 물려받는다. 시·도가 바뀌면 그 뒤는 바뀐 시·도를 따른다 |
| 41 | ✅ | 자체 | `lib/saramin.mjs` `check_alive.mjs` | 사람인 목록 40건 중 **9건(22%)이 마감일 없는 상시채용·채용시** 공고다. 날짜 경과로 마감 판정하면 이 22%가 통째로 죽는다 → 상태 블록 4형태로만 판정 |
| 42 | ✅ | 자체 | `lib/merge.mjs` | 실측 교차 보드 중복 1건이 **제목 수식어 차이로 유사도 0.64** 가 나와 병합이 사실상 0건이 됐다(`"팀원 (부산)"` vs `"팀원 (신입~5년 이하 / 부산)"`). 임계값을 낮추는 대신 **경력·지역·고용형태 표기만** 떼고 비교한다 — "(커머스)" vs "(물류)"는 여전히 다른 자리 |
| 43 | ✅ | 자체 | `collect_saramin.mjs` | 공고당 상세·본문 2요청 × 1초라 300건이면 10분이다. `--max`(기본 200) 상한을 두되 **잘랐다는 사실을 manifest 와 리포트에 남긴다** |
| 44 | ✅ | 자체 | `collect_saramin.mjs` `render.mjs` | 보드가 둘인데 `lastRun` 하나를 공유하면 **사람인 수집이 원티드의 부분 실패 경고를 덮어쓴다** → 보드별 `runs.{board}` 로 분리 |
| 45 | ✅ | 자체 | `render.mjs` | 켜 놓고 **한 번도 안 돌린 보드**가 있으면 그 보드의 공고는 존재 자체가 안 보인다 → 리포트 상단 경고 |
| 46 | ✅ | 자체 | `finance.mjs` `resolve_company.mjs` | 동명이인을 **물어보기만 하고 답을 저장할 곳이 없었다.** 같은 질문이 매 실행 반복됐다 → `company_choices.json` 에 저장하고 법인등록번호를 힌트로 주입. "모르겠음"도 결정으로 저장 |
| 47 | ✅ | 자체 | `resolve_company.mjs` | 결정을 저장해도 **재무 캐시에 옛 결과가 남아** 다음 실행이 그걸 그대로 쓴다 → 결정과 함께 해당 회사 캐시를 지운다 (판정·저장·기존데이터 3단 수정) |
| 48 | ✅ | 자체 | `test/run.mjs` | 병합 가드 테스트가 `why` 만 보고 `same` 을 안 봐서 **뮤테이션 검증에서 안 물었다**(지역 판정을 뒤집어도 통과). 판정 자체를 함께 검사하도록 고침 |
| 49 | ✅ | 자체 | `test/integration.mjs` | `fetch` 는 `Host` 를 금지 헤더로 막아 **DNS 리바인딩 검사를 테스트할 수 없었다** → raw `http.request` 로 검사 |
| 50 | ✅ | 자체 | `collect_*.mjs` | 손으로 추가한 공고(`pinned`)는 애초에 검색 조건 밖이라 넣은 것인데 수집이 `stale`(조건 밖) 로 표시했다 → 제외 |

---

## 🔴 P1 — 배포 준비 중 드러난 것 (2026-08-10)

| # | 상태 | 출처 | 위치 | 내용 |
|---|---|---|---|---|
| 51 | ✅ | 사용자 지적 | `profile.example.yml` · `gate.mjs` · `SKILL.md` | 🔴 **제품이 12년차 시니어를 기본값으로 깔고 있었다.** 온보딩 화면에 `10년 미만 공고는 따로 모음`이 그대로 떴다. **판정 로직은 멀쩡했다** — 언제나 내 연차 대비 상대 비교였다. 망가진 것은 기본값·예시·태그 이름이었다. `README` 는 "어떤 직군이든"이라고 하는데 **어떤 연차든은 아니었다** |
| 52 | ✅ | 자체 | `gate.mjs` | 태그 이름 `junior`/`stretch` 가 **절대 연차 표현**이었다. 2년차에게 신입 공고는 "주니어 공고"가 아니라 그냥 맞는 공고다 → `belowMyLevel`/`aboveMyLevel` |
| 53 | ✅ | 자체 | `profile.example.yml` | 요구경력 하한 기본값 `10` — **신입이 이 템플릿을 복사하면 거의 모든 공고가 "따로 모음"으로 빠진다.** 정작 볼 것이 안 보이는데 오류는 하나도 안 난다 → 기본값 제거(미설정 = 분류 안 함) |
| 54 | ✅ | 자체 | `test/run.mjs` | 🔴 **연차 분류에 테스트가 하나도 없었다.** 태그 이름을 통째로 바꿔도 317건 중 아무것도 안 깨졌다 → 15건 추가. 라벨에 절대 연차 표현이 되돌아오면 걸린다 |
| 55 | ✅ | 자체 | `profile.example.yml` | 예시 프로필에 **제작자의 실제 자택 지역과 좌표**가 들어 있었다. 공개 저장소에 그대로 나갈 뻔했다 → 일반 예시로 교체 |
| 56 | ✅ | 자체 | `.gitignore` | `docs/` 패턴이 **저장소 문서까지 막고 있었다.** 의도는 사용자 데이터 디렉터리 차단인데 `IMPROVEMENTS.md` 가 통째로 커밋에서 빠질 뻔했다 → 경로 정밀화 |
| 57 | ✅ | 자체 | `finance.mjs` · `pubdata.mjs` · `SKILL.md` | 키 안내가 **제작자 개인 컴퓨터의 스크립트**(`kw-key.sh`)를 가리키고 있었다. 남의 컴퓨터에서 안 도는 안내다 → 표준 환경변수 `DATA_GO_KR_KEY` |

## 🔴 P1 — 공개 직후 외부 검증에서 드러난 것 (2026-08-10)

> 공개 후 **처음 보는 사람 관점**으로 외부 모델에 전체를 검증시켰다. 첫 지시는
> "문서가 코드에 대해 거짓말하는 곳을 찾아라"였다. 7건이 나왔고 전부 사실이었다.

| # | 상태 | 출처 | 위치 | 내용 |
|---|---|---|---|---|
| 58 | ✅ | 외부검증 | `.claude-plugin/marketplace.json` | 🔴 **README 의 첫 설치 명령이 그대로 깨져 있었다.** `marketplace add` 는 마켓플레이스 목록 파일을 찾는데 저장소에는 `plugin.json` 만 있었다. 처음 써 보는 사람이 첫 줄에서 막히고, **자기가 뭘 잘못했는지 알 방법이 없다** → `marketplace.json` 추가 후 실제 설치까지 실측 확인 |
| 59 | ✅ | 외부검증 | `references/profile.example.yml` | 🔴 통근 상한을 `초과분은 목록에 아예 올라오지 않는다`고 약속했다. **통근 실측은 미구현이다.** #21 을 "✅ 완료"로 마감했는데 이 파일만 빠진 **회귀**였고, 신규 사용자가 가장 먼저 복사하는 파일이라 노출 확률이 가장 높았다 → `maxCommuteMin`·`maxTransfers`·`hybridFactor`·`precise` 전부 ⏳ 표기 |
| 60 | ✅ | 외부검증 | `SKILL.md` frontmatter | 스킬 설명문이 `통근 시간 확인에 사용한다`고 광고했다. **에이전트가 스킬을 고르는 근거 문장**이라, "통근 얼마나 걸려?"에 발동해서 못 한다고 답하는 구조였다 |
| 61 | ✅ | 외부검증 | `SKILL.md` 등급표 | `자본총계 < 0` 으로 적혀 있는데 코드는 `<= 0`(`lib/grade.mjs:65`). 이 `≤` 가 바로 값을 치르고 고친 경계값(#8)이다 — **고쳐 놓고 문서에 반영을 안 했다** |
| 62 | ✅ | 외부검증 | `SKILL.md` · `test/run.mjs` | 테스트 건수가 문서마다 달랐다(293 vs 308). "근거 없는 숫자를 말하지 않는다"를 내세운 저장소에서 자기 숫자가 갈렸다. 이미지 본문 표본도 `4건 중 2건` vs `14건 중 3건`으로 어긋났다 |
| 63 | ✅ | 외부검증 | `profile.example.yml` · `SKILL.md` | 사람인 **HTML 파싱을 `api` 로 분류**했다. `api` 를 "공식 API"라고 스스로 정의해 놓고 공개 페이지 파싱을 같은 이름에 넣어, **동의 절차가 필요한 방식과 아닌 방식의 경계가 흐려졌다** → `web` 모드 신설 |
| 64 | ✅ | 자체 | `lib/io.mjs` · `collect_*.mjs` | 🔴 63 을 고치다 발견. 수집 스크립트가 `sources` 값을 **`=== 'off'` 로만 봤다.** `offf` 같은 오타를 적으면 **사용자는 껐다고 믿는데 계속 긁는다** → `requireSourceEnabled()` 로 아는 값만 통과. 테스트 9건 추가 |
| 65 | ✅ | 외부검증 | `profile.example.yml` | 자택 주소 주석이 "좌표로만 저장되고"라고 적혀 있는데 **바로 그 줄에 주소 원문이 저장된다.** #23 에서 고친 문구의 회귀. README 는 올바르게 적고 있어 두 문서가 서로 다른 말을 했다 |

> 58~65 의 공통점 — **전부 문서였고, 전부 자기가 세운 규칙을 자기가 어긴 것**이다.
> 코드는 규칙을 지키고 있었는데 문서가 못 따라갔다. 규칙을 크게 적어 두는 것만으로는 안 지켜진다.

## 🔴 P1 — 확보율 재측정(M5-C) 중 드러난 것 (2026-08-10)

| # | 상태 | 출처 | 위치 | 내용 |
|---|---|---|---|---|
| 66 | ✅ | 자체 | `collect_saramin.mjs` | 🔴 **세어 보지도 않고 "원티드와 겹치는 공고가 있습니다"를 매번 단정**했다. 원티드를 `off` 로 두고 사람인만 150건 받은 실행에서도 그대로 떴다 — 겹칠 대상이 0건인데 병합을 돌리라고 한다. 이 저장소는 "잘린 사실을 적는다"·"모르면 모른다고 적는다"를 내세우는데, **확인하지 않은 사실을 도구가 단정하면 나머지 경고도 같이 못 믿게 된다** → 다른 보드 공고 수를 실제로 세고, 0이면 아무 말도 하지 않는다 |
| 67 | ✅ | 자체 | `lib/text.mjs` | 사람인은 국문·영문 상호를 **슬래시로 병기**한다(`(주)아이클레이브 / iclave`). 괄호 병기(`미소(Miso)`)는 쪼개면서 슬래시는 안 쪼개, **그 이름의 법인은 어디에도 없어** 모든 후보가 빗나가고 조용히 미확인이 된다 → 공백으로 둘러싸인 슬래시만 분해. 이름 속 슬래시(`에이/비스토어`)를 쪼개면 없는 회사를 만들어 내므로 그건 그대로 둔다. 테스트 3건 추가 (317 → 320) |

> 67 은 **이번 표본에서는 실제 손실이 0이었다.** 해당 2곳을 국문 상호로 직접 조회해 봤더니
> 애초에 DART 미등록이었다. 고친 이유는 손실을 봐서가 아니라 **손실이 났어도 알 수 없는 구조**였기 때문이다.

## 🔴 P0 — 배포 후 첫 사용자 제보 (2026-08-18)

받은 것은 알림 한 줄이었다 — `오늘 채용 스크리닝 실패 — 사람인 접근이 막혀 연봉 검증 불가, 추천 0건.`

| # | 상태 | 출처 | 위치 | 내용 |
|---|---|---|---|---|
| 68 | ✅ | 사용자 | `gate.mjs:37` | 🔴 **수집이 0건이면 사유를 말하지 않고 `exit 1` 로 끊었다.** 그 0건이 차단 때문인지 조건이 좁아서인지 사용자는 알 방법이 없고, **리포트 자체가 만들어지지 않아** 경고를 실어 둔 화면에 도달하지도 못한다. 차단 사실은 바로 옆 `postings.json` 의 `runs.saramin.queries[].error` 에 이미 적혀 있었는데 gate 가 읽지 않았다 → 0건이어도 사유·대처를 적고 **빈 결과로 다음 단계에 넘긴다.** 단 수집 기록이 아예 없으면(=한 번도 안 돌림) 그때만 멈춘다 |
| 69 | ✅ | 사용자 | `render.mjs:115` `serve.mjs:113` | 실패를 **`조회 실패` 넉 자**로만 적었다. HTTP 상태도, 차단인지 네트워크 단절인지도 버렸다. 같은 로직이 `render`·`serve` 두 곳에 복사돼 있었고 이미 문구가 갈라져 있었다(상세 잘림 문장이 서로 다름) → `lib/runstatus.mjs` 하나로 합치고 콘솔·리포트·serve 가 **같은 문구**를 쓰게 했다 |
| 70 | ✅ | 사용자 | `lib/http.mjs` | 403·429·`fetch failed` 을 전부 같은 실패로 다뤘다. **셋은 대처가 서로 다르다** — 차단은 실행 환경을 바꿔야 하고, 한도는 기다려야 하고, 네트워크는 연결 문제다. 그중 **차단만이 사용자가 고칠 수 있는 것**인데 그 말을 해 주지 않았다 → `diagnose()` 로 종류·라벨·대처를 분류. 네트워크 오류를 `new Error(mask(e.message))` 로 다시 싸느라 원인 코드를 잃던 것도 함께 고침(`NetworkError`) |

| 71 | ✅ | 자체 | `lib/http.mjs` | 🔴 68~70 을 검수하다 발견. **회사망·클라우드 방화벽은 CONNECT 를 403 으로 되받는다.** 그러면 `fetch` 는 그저 `fetch failed` 라고만 해서 도구가 `네트워크 연결 실패` 로 읽었다 — 사용자에게 **"인터넷 연결을 확인하십시오"** 라고 말하게 된다. 정작 인터넷은 멀쩡하고 막힌 것이라 그 안내는 사용자를 엉뚱한 곳으로 보낸다 → `cause` 사슬을 훑어 `Proxy response (403)` 을 잡아내고 `중간 프록시 차단` 으로 따로 분류. 로컬 프록시로 실제 재현해 collect → gate → render 를 끝까지 돌려 확인 |
| 72 | ✅ | 자체 | `render.mjs` `serve.mjs` | 차단된 보드에 **"아직 수집하지 않았습니다"** 라고 적었다. 바로 위에 차단 경고를 띄워 놓고서다 — 사용자는 자기가 안 돌린 줄 알고 **같은 실행을 반복**한다 → 실행 기록이 있는 보드는 "안 돌림"에서 뺀다 |

> **68~72 의 공통점** — 도구는 무엇이 잘못됐는지 **알고 있었다.** 알고도 사용자에게 전달하지 않았다.
> 이 저장소가 "조용한 손실이 가장 나쁜 실패"라고 적어 둔 그 실패를, **실패 보고 경로 자체가** 저지르고 있었다.
> 배포 전에 못 잡은 이유도 분명하다 — 테스트가 전부 **성공 경로**를 돌렸다. 차단된 채로 파이프라인을 끝까지 돌려 본 적이 없다.
> 회귀 테스트 43건 추가 (338 → 381) · 투자 정보로 34건 (→ 415) · 외부 검증 반영으로 27건 (→ 442), 그중 9건은 차단 상태로 `gate → render` 를 실제 프로세스로 돌려, 리포트에 사유·대처가 실리고 막힌 보드를 "안 돌림"으로 적지 않는지 확인한다.

### 확인하지 못한 것

- **사용자 쪽이 정확히 무엇에 막혔는지는 모른다.** 같은 시각 이 컴퓨터에서 사람인 목록·상세는 200 으로 정상이었다(실측 73건 수집). 클라우드·회사망 등 실행 환경 차이로 보이나 로그를 받지 못해 단정하지 않는다.
- 제보 문구의 **"연봉 검증"** 은 이 도구에 없는 기능이다(판정하는 것은 재무 등급). 요약이 바꿔 적은 것인지 다른 도구인지 미확인.
- `view-ajax` 는 **Referer 가 없으면 404** 를 준다(2026-08-18 실측). 코드는 이미 보내고 있어 이번 원인은 아니지만, 사람인이 헤더 정책을 바꾸면 상세 조회가 전량 죽는 지점이다.

## 🔴 P1 — 투자 정보 (2026-08-18 CEO 지시)

| # | 상태 | 출처 | 위치 | 내용 |
|---|---|---|---|---|
| 73 | ✅ | CEO | `lib/investment.mjs` `lib/grade.mjs` `finance.mjs` | **재무제표는 작년 이야기다.** 자본잠식으로 찍힌 회사가 올해 유상증자를 받았으면 상황이 다른데, 등급은 그 사실을 전혀 못 봤다 → DART 공시 목록(재무 찾느라 이미 받아 둔 것)에서 조달 사건을 뽑아 등급·리포트·면접 질문에 반영. 실측 확인: 비바리퍼블리카 8건 · 웹케시 0건 · 당근마켓 0건 |

> 🔴 **이 기능에서 제일 위험한 것은 "비슷한 이름의 반대 사건"이다.**
> `무상증자`(회계 처리, 돈 안 들어옴) · `자기주식 취득`·`주식 소각`·`유상감자`(돈이 **나감**) ·
> `타법인 주식 취득`(회사가 **남에게** 투자) · `대량보유상황보고`(주주끼리 매매).
> 이것들을 투자로 세면 **돈이 나간 회사가 돈을 받은 회사로 보인다.** 제외 패턴을 조달 패턴보다 **먼저** 돌리고,
> 테스트가 6건 전부를 물고 있다. 회귀 34건 추가 (381 → 415).

### 외부 검증(codex)에서 걸린 7건 — 전부 반영 ✅

착수 직후 codex 리뷰를 돌렸다. **7건 전부 "돈이 안 들어왔는데 들어온 것으로 세는" 경로**였다.

| # | 상태 | 출처 | 위치 | 내용 |
|---|---|---|---|---|
| 74 | ✅ | codex | `lib/investment.mjs` | `소액공모` 를 무조건 지분 조달로 셌다. **`소액공모공시서류(채무증권)`** 이면 회사채 발행인데 자본잠식 등급이 완화된다 → 지분·채무를 나누고, **종류가 안 적혔으면 지분으로 치지 않는다** |
| 75 | ✅ | codex | `lib/investment.mjs` | **철회·취소된 증자와 종속회사 증자**를 이 회사 조달로 셌다. 되돌린 결정은 돈이 안 들어왔고, 자회사 증자는 이 회사 돈이 아니다 → 제외 패턴에 추가. `[기재정정]` 은 같은 사건을 다시 낸 것이라 살려 둔다 |
| 76 | ✅ | codex | `lib/investment.mjs` `lib/grade.mjs` | 완화 문구가 **목록의 최신 공시**를 가리켰다. 유상증자 뒤에 전환사채가 오면 "전환사채 공시 — 위험에서 경고로 낮췄습니다"가 뜬다 — **"전환사채는 위험을 내리지 못한다"고 적어 놓고 전환사채를 근거로 내미는 꼴** → `recentEquityEvent` 로 완화의 근거가 된 공시를 따로 들고 다닌다 |
| 77 | ✅ | codex | `lib/freshness.mjs`(신규) `finance.mjs` | 캐시 신선도를 **회계연도만으로** 판정했다. 조달 공시는 주기가 달라서, 다음 달 유상증자가 올라와도 회계연도는 그대로다 → **새 조달이 영영 안 붙고 위험 등급이 굳는다.** 기능 이전에 만들어진 캐시(`investment` 필드 없음)도 영원히 신선했다 → 투자 정보를 켠 동안에는 7일 주기 재검증, 옛 캐시는 즉시 무효. 판정 규칙은 순수 함수로 빼서 테스트가 물게 했다(#66 과 같은 이유) |
| 78 | ✅ | codex | `lib/investment.mjs` | **미래 날짜 공시가 자동으로 "최근 조달"** 이 됐다(`months` 가 음수인데 `<= 12` 만 봤다). 날짜를 잘못 읽으면 그 오류가 곧바로 등급 완화가 된다 → `months >= 0` 추가 |
| 79 | ✅ | codex | `lib/http.mjs` | `diagnose()` 가 **cause 안쪽의 `TimeoutError`** 를 못 봤다. `fetch failed → cause: TimeoutError` 를 네트워크 단절로 읽어, 사용자에게 "잠시 뒤 재시도" 대신 **"인터넷 연결을 확인하라"** 는 엉뚱한 지시를 했다 → 이름 판정도 사슬 전체에 적용 |
| 80 | ✅ | codex | `SKILL.md` `README.md` | 문서가 DART 추가 조회를 **"회사당 2건"** 이라고 단정했다. `resolveDartCorp()` 는 이름 변형별로 검색하고 후보별 개황을 봐서 동명이인이 있으면 더 든다 → "최소 2건, 이름 변형·동명이인이면 더 늘어난다"로 정정 |

> 회귀 27건 추가 (415 → 442). 지적된 6개 경로(채무 소액공모 · 철회/종속회사 · 최신 CB+이전 증자 · 옛 캐시 · 미래 날짜 · 중첩 TimeoutError)를 전부 테스트가 문다.

### 결정 (2026-08-18)

| # | 사안 | 결정 | 근거 |
|---|---|---|---|
| D26 | 조달 **금액**을 읽을 것인가 | **읽지 않는다. 제목까지만 사실로 다루고 금액은 링크로 넘긴다** | 원문을 열면 회사당 요청이 한 번 더 늘고, 공시 서식이 제각각이라 금액 오추출이 **곧바로 잘못된 등급**이 된다. 대신 ① 등급을 올리는 폭을 한 단계로 묶고 ② "금액은 공시 원문에서 확인해 주십시오"를 문구에서 빼지 않고 ③ 리포트에 공시 원문 링크를 붙였다 — **링크가 곧 근거다** |
| D27 | 조달이 등급을 얼마나 움직이는가 | **`위험 → 경고` 한 단계, 그것도 최근 12개월 안의 지분 조달일 때만** | 금액을 모르는 채로 위험을 양호로 올리면 5천만 원 증자가 자본잠식을 지운다. 경고를 양호로 올리지도 않는다 — **돈이 들어온다고 적자가 흑자가 되지 않는다** |
| D28 | 전환사채도 셀 것인가 | **사실로는 싣되 등급은 움직이지 못한다** | 부채로 들어온 돈이다. 자본잠식 판정을 뒤집을 근거가 아니다 |
| D29 | 공공데이터포털로 재무가 끝난 회사도 DART를 볼 것인가 | **본다 (회사당 2요청 추가). `finance.investment: false` 로 끌 수 있다** | 안 보면 **공공데이터포털로 해결된 회사만 투자 정보가 통째로 비는** 구조가 된다. 어떤 회사는 있고 어떤 회사는 없는 이유를 사용자가 알 방법이 없다 |

---

### 같이 한 것 (2026-08-18)

- **설명 화면 전량 재생성** — Playwright 로 실제 렌더러·실제 실행 출력을 다시 찍었다. 새로 만든 것 4장: `install-1-prepare` · `install-2-plugin` · `key-1-datagokr` · `onboarding-4-blocked` · `report-blocked`.
  `onboarding-4-blocked` 와 `report-blocked` 는 **로컬 프록시로 차단을 재현한 뒤 실제로 돌려 받은 출력**이다 — 이 결함의 수정이 화면에서 어떻게 보이는지가 곧 증거다.
- **README 를 초보자용 0~6단계로 재작성** — Node·Claude Code 유무 확인부터, 없을 때 받는 법, 키 발급 5단계, 환경변수 넣는 법(macOS·Windows), 확인 명령까지. 단계마다 화면을 붙였다.
- ✅ **재무등급의 정의 — 해소.** CEO 확인 결과 재무등급이 맞고(연봉 아님), 정의는 `최신 재무제표 + 투자정보 리서치` 다. 같은 날 투자 정보를 구현해 붙였다 (아래 #73 · D26).

### 확보율 재측정 결과 (2026-08-10)

사람인 전국 · 공고 150건 · 회사 135곳. `--max 150`(직전 측정은 표본 14건이었다).

| | |
|---|---|
| 확보 | **66/135 (48.9%)** |
| 등급 분포 | 좋음 12 · 양호 35 · 경고 18 · 위험 1 · 미확인 69 |
| 출처 | 공공데이터포털 36 · DART 감사보고서 27 · 둘 다 3 |
| 미확인 사유 | DART 미등록 60 · 2023년 이후 공시 없음 6 · 동명이인 1 · 자료 노후 1 · 항목 부족 1 |

🔴 **보드가 다르면 확보율도 다른데, 원인은 파서가 아니라 그 보드에 올라오는 회사다.**
원티드 표본은 미확인 15곳 중 4곳(27%)이 동명이인이었는데 사람인 표본은 1곳뿐이고,
대신 **DART 미등록이 69곳 중 60곳(87%)** 이다. 외부감사 대상이 아닌 규모의 회사가 그만큼 많다는 뜻이다.
채용대행·헤드헌팅으로 보이는 상호는 미확인 쪽에 12곳, 확보 쪽에 2곳 — 쏠려 있지만 미확인의 17%만 설명한다.
**확보율을 한 숫자로 말하면 안 되는 이유가 이 표에 있다.**

> 66 은 화면에 찍히는 문장 하나라 **회귀 테스트로 잡히지 않는다.** 스크립트 본문의 `console.log` 는
> 테스트에서 부를 수 있는 함수가 아니다. 이런 문장이 몇 개나 더 있는지는 아직 세어 보지 않았다 —
> #54(연차 분류에 테스트 0건)와 같은 종류의 빈틈으로 남겨 둔다.
> 지킨 걸 **검사하는 장치**가 있어야 한다 — 그래서 나온 것들은 가능한 한 테스트로 옮겼다(#64).

---

> 51~54 는 같은 뿌리다 — **만든 사람이 곧 사용자일 때 생기는 사각지대.**
> 자기한테 맞는 값이 기본값으로 굳고, 자기가 안 겪는 경우에는 테스트가 안 생긴다.
> 55~57 도 같은 성질이다. 자기 컴퓨터에서는 전부 잘 돌았다.

---

## 📌 결정이 필요한 설계 구멍 (codex 지적 + 판단)

| # | 사안 | 결정 | 근거 |
|---|---|---|---|
| D1 | `u`를 "데이터 없음"으로만 쓸지, "규칙상 판정 불가"를 따로 둘지 | **`u` 하나로 두되 사유를 반드시 병기**한다. 등급을 늘리면 사용자가 외워야 할 것이 늘어난다 | 사용자에게 필요한 건 등급 개수가 아니라 "왜 없는가"다 |
| D2 | 법인번호와 상호·지역이 충돌할 때 | **법인번호 > 상호 > 지역.** 번호가 있는데 불일치면 즉시 실패, 지역은 동명이인을 좁힐 때만 보조로 | 번호는 겹치지 않고 상호는 얼마든지 겹친다 |
| D3 | 연결·별도 혼합 | **별도 우선, 시계열은 같은 기준끼리만.** 기준이 섞이면 추세 판정을 하지 않고 최신 1개년만 표시 | 연결·별도를 비교하면 매출 성장이 회계 기준 변경으로 만들어진다 |
| D4 | 공시 캐시 갱신 주기 | **회계연도 기준 재검증.** 캐시된 최신 회계연도가 "지금 나와 있어야 할 연도"보다 낡으면 다시 조회. 한국 12월 결산은 4월에 감사보고서가 나오므로 5월 이후 기준 | 날짜 TTL은 공시 주기와 무관해 헛조회 또는 낡은 값을 만든다 |
| D5 | "전수조사"의 완료 조건 | **부분 성공을 부분 성공이라고 표시한다.** 키워드별·보드별 성공/실패/잘림을 manifest에 남기고 리포트 상단에 경고 | 조용한 부분 실패가 이 제품에서 가장 나쁜 실패다 |
| D6 | 사라진 공고·마감·재공고의 상태 모델 | `active` / `closed`(마감 확인) / `stale`(현재 검색 조건 밖) 3상태. **삭제하지 않는다** | 재공고는 새 ID로 올라온다. 지우면 이력이 끊긴다 |
| D7 | 지역 필터 vs 통근 필터 | **통근이 최종 기준, 지역은 사전 컷.** 통근 API 실패는 `drop`이 아니라 `hold` | 추측으로 공고를 버리지 않는다 |
| D8 | 개인용 vs 공유용 리포트 | **기본이 공유 안전.** 지원 상태는 `--with-status`로만 포함 | 실수로 새는 쪽이 실수로 빠지는 쪽보다 훨씬 비싸다 |
| D9 | 재공고를 "같은 자리"로 이을 기준 | **제목 유사도 ≥0.8이 정확히 1건일 때만.** 여럿이면 "이 회사에 살아있는 공고 N건"으로 격하 | 잘못 이으면 남의 자리에 지원 이력이 승계된다. 부분일치 5건 사고와 같은 원칙 |
| D10 | 재검색에서 회사를 특정하는 키 | **원티드 회사 ID(`boardId`) 완전일치.** 없을 때만 정규화 상호 완전일치 | 회사명 `includes`는 "미소 → 미소무역" 함정을 그대로 재현한다 |
| D11 | 되찾은 공고의 범위 | **같은 자리로 연결된 것 + 내 직군 키워드에 걸리는 것만.** 나머지는 건수·제목만 알린다 | 회사의 모든 공고를 끌어오면 되찾기가 잡음 주입이 된다 |
| D12 | 지원 이력 승계 | **자동 승계하되 원 공고 키를 `history`에 보존** | 끊기면 같은 회사에 두 번 지원한다 |

### M4 에서 정한 것 (2026-08-10)

| # | 사안 | 결정 | 근거 |
|---|---|---|---|
| D13 | 사람인 수집 경로 — 오픈API(키 1개) vs 공개 웹 | **공개 웹 목록 파싱.** CEO 확정 | 새 키는 "필수 키 1개" 첫인상 약속을 깬다 — commute 를 막은 것과 같은 기준. robots 는 세 경로 모두 허용하나 GPTBot 차단 사실을 문서에 명시하고 1초 간격·개인 용도로 제한 |
| D14 | 사람인의 "전수조사" 완료 조건 | **제목 매치율 바닥에서 멈추고, 멈춘 지점과 이유를 리포트에 적는다** | 형태소 확장으로 한 단어에 1만 건이 나와 전수가 성립하지 않는다. 페이지 상한은 자의적이고, 매치율은 실측 근거가 있다(p4에서 31%로 꺾임) |
| D15 | 병합이 불확실할 때 | **합치지 않고 "중복일 수 있음"으로 표시만 한다** | 안 합치면 중복이 눈에 보이지만, 잘못 합치면 서로 다른 자리 하나가 조용히 사라진다. 이 제품은 조용한 손실을 가장 나쁜 실패로 본다 |
| D16 | 병합 그룹의 대표 | **살아있는 쪽 → 좌표 있는 쪽 → JD 원문 있는 쪽 → 먼저 수집된 쪽** | 한 보드에서 내려가고 다른 보드엔 남아 있는 일이 흔하다. 마감된 쪽을 대표로 세우면 산 자리가 마감으로 보인다 |
| D17 | 병합 그룹의 게이트 판정 | **가장 관대한 판정을 그룹 전체에 적용** | 보드마다 근무지 표기가 달라 한쪽만 통과하는 일이 있다. 추측으로 공고를 버리지 않는다 |
| D18 | 제목 유사도 임계값을 낮출 것인가 | **낮추지 않는다. 대신 조건 표기(경력·지역·고용형태)만 떼고 비교한다** | 0.8 은 재공고 오연결을 막는 값이다. 실측 실패 원인은 임계값이 아니라 비교 대상이었다 — 경력·지역은 이미 따로 대조하고 있어 두 번 세고 있었다 |
| D19 | serve 의 "삭제" | **삭제하지 않는다. 목록에서 숨기고 기록·JD 원문은 남긴다** | 같은 자리가 새 ID로 재공고된다. 지우면 이력이 끊겨 같은 회사에 두 번 지원한다 (D6 와 같은 원칙) |
| D20 | serve 인증 | **127.0.0.1 바인딩 + 매 실행 랜덤 토큰 + Host 검사 + 쓰기 Origin 검사** | 이력서·자택주소·지원이력이 있는 디렉터리를 여는 서버다. 브라우저에 떠 있는 아무 페이지나 localhost 로 요청을 던질 수 있다 |
| D21 | 편집 화면을 따로 만들 것인가 | **만들지 않는다. `report.html` 하나에 편집 계층만 얹는다** | UI 가 두 벌이 되면 한쪽만 고쳐지고 둘이 갈라진다 |
| D22 | 동명이인에서 "모르겠음" | **그것도 결정으로 저장한다** | 저장하지 않으면 같은 질문이 매 실행 반복된다. 되돌리기(`--reset`)를 함께 둔다 |

---

## 다음 (M5 잔여)

M4 3개 항목은 모두 구현했다(사람인 수집 + 교차 병합 / `serve` / 동명이인 확인).
M5-A(패키지)·M5-B(CI·기여 안내·데모 화면)·M5-C(확보율 재측정)·M5-D(연차 중립화)는 완료했다. 남은 것:

1. **잡코리아 수집** — 세 번째 보드. 사람인에서 만든 보드 어댑터 구조를 그대로 쓴다.
   선행: `og:description` 마감 신호 실측 + 이미지 본문 비율 측정
2. 리포트에서 JD 원문 열기 (지금은 파일 경로만 남는다)

### M5-C 재측정에서 올라온 결정 2건 — ✅ 확정 (2026-08-10)

| # | 사안 | 결정 | 근거 |
|---|---|---|---|
| D23 | 상세 조회 상한의 기본값 | **첫 실행은 상한 없이 전량. 다음 실행부터 200.** 전량 조회는 **시작 전에 건수와 예상 시간을 알린다** | 재측정에서 목록 유니크 1035건이 나왔고 기본값 200은 그중 19%다. **처음 만들어진 목록이 5분의 1이면 사용자는 그 5분의 1을 자기 시장 전부로 읽는다.** 잘린 사실을 리포트에 적어 두어도 첫 화면이 만드는 오해가 더 세다 — 가장 비싼 오해가 가장 먼저 생긴다. 대신 말 없이 30분을 쓰지는 않는다 |
| D24 | 상한이 세는 대상 | **실제로 받아야 하는 건수만 센다.** 이미 받아 둔 공고는 상한에서 제외 | 후보 건수를 세면 재실행 때 앞 200건이 전부 캐시라도 예산이 소진돼 **목록 뒤쪽의 새 공고가 영영 안 받아진다.** 조용한 손실이 상한 뒤에 쌓이는 구조였다 |
| D25 | 리포트 기본 정렬 | **좋음 먼저.** `위험 먼저`를 별도 항목으로 남기고, 라벨이 방향을 말하게 한다. 미확인은 양쪽 다 맨 뒤 | 이 화면에서 사용자가 하려는 일은 **지원할 곳을 고르는 것**이다. 위험을 맨 위에 올리면 경고는 잘 보이지만 정작 볼 것이 바닥에 깔린다. `등급순`이라는 라벨은 방향을 말해 주지 않아 어느 쪽이든 오해를 만든다 |

> D23·D24 규칙은 `lib/saramin.mjs` 의 `planDetailBudget` 에 순수 함수로 뺐다.
> **#66 에서 "스크립트 본문의 `console.log` 는 테스트가 못 문다"고 적어 놓고 같은 자리에 규칙을 또 둘 수는 없다.**
> 테스트 11건(첫 실행 전량 / 재실행 상한 / 명시 우선 / 캐시 제외 / 목록 뒤쪽 새 공고 / `--max 0`)을 붙였다.
> D25 는 템플릿 문자열이라 통합 테스트에서 정렬 상수와 라벨을 함께 검사한다 — `등급순`이 되돌아오면 걸린다.

🔴 **`commute.mjs`는 여전히 착수하지 않는다 (Fable 판정 유지).** 유일하게 새 키 2개를 요구해
"필수 키 1개" 첫인상 약속을 깨고, ODsay 1,000/일 한도 대비 원티드 좌표 + 지역 필터가
이미 대체 신호를 준다. **지금 손해가 발생하고 있지 않은 유일한 후보다.**
사람인이 좌표를 주지 않는다는 사실이 이 판정을 더 굳힌다 — 두 보드 중 하나는 어차피 정밀 측정이 안 된다.
