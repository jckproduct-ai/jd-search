# 기여 안내

## 가장 환영하는 기여: `role-presets.yml`

[`skills/jd-search/references/role-presets.yml`](skills/jd-search/references/role-presets.yml)이
이 저장소에서 **가장 커뮤니티에 열려 있는 파일**입니다. 직군별 검색 키워드 묶음인데,
한국 잡보드에서 실제로 쓰이는 표기를 아는 사람이 그 직군의 종사자뿐이기 때문입니다.

> 예시 프로필에 `프로덕트 오너`가 빠져 있어서 **PO 공고 17건이 아무 오류 없이 사라지고 있었습니다.**
> 키워드 하나의 누락이 곧 조용한 손실입니다. 자기 직군이 없거나 키워드가 부실하면 PR을 보내 주십시오.

```yaml
my-role:
  label: 내 직군
  keywords: [한글표기, 띄어쓰기 없는 표기, English, 약어]
  exclude: [키워드가 겹쳐 딸려 오는 다른 직무]
```

- **한글·영문 표기를 둘 다 넣으십시오.** `Product Manager`만 있으면 `프로덕트 매니저` 공고를 통째로 놓칩니다
- **띄어쓰기 변형도 넣으십시오.** `서비스기획`과 `서비스 기획`은 다른 문자열입니다
- 영문 약어(`PM`·`PO`)는 낱말 경계로 매칭하므로 `PMO`·`POC`에 걸리지 않습니다

---

## 코드 기여

### 테스트

```bash
node skills/jd-search/scripts/test/run.mjs
```

**네트워크를 쓰지 않습니다.** 통합 테스트까지 함께 돕니다. PR 전에 반드시 통과시켜 주십시오.

### 이 저장소의 규칙 셋

이 셋은 취향이 아니라 **실제로 값을 치르고 배운 것**이라, PR에서 지켜 주셔야 합니다.
배경은 [`docs/DEVLOG.md`](docs/DEVLOG.md)와 [`docs/IMPROVEMENTS.md`](docs/IMPROVEMENTS.md)에 있습니다.

**1. 추측으로 공고를 버리지 않습니다.**
판정은 pass/drop 둘이 아니라 **unknown을 포함한 셋**입니다.
확실히 범위 밖일 때만 버리고, 판정 못 하면 남긴 채 사용자에게 알립니다.
`"서울"`처럼 광역만 적힌 공고를 구 단위 조건과 비교해 떨어뜨리면 멀쩡한 자리가 조용히 사라집니다.

**2. 못 한 일은 반드시 표시합니다.**
수집이 한도에서 잘렸거나, 키워드 조회가 실패했거나, 켜 놓고 안 돌린 보드가 있으면
**리포트 위쪽에 경고로 뜹니다.** 부분 성공을 전수로 보여주는 코드는 받지 않습니다.

**3. 보드별 판정 신호를 하나로 묶지 않습니다.**
마감 판정은 보드마다 완전히 다릅니다. 공통 규칙으로 통일하면 **둘 중 한쪽은 반드시 틀립니다.**
레코드 모양은 같게 맞추되 판정 로직은 어댑터 안에 두십시오.

### 새 보드를 추가하려면

`lib/wanted.mjs`·`lib/saramin.mjs`가 참고 구현입니다. 필요한 것:

1. `parseList` / `parseDetail` / `normalize` / `jdMarkdown`
2. `check_alive.mjs`의 `ADAPTERS`에 등록 (`check` / `searchCompany` / `record`)
3. **실물 HTML fixture**와 그에 대한 테스트 — 특히 **마감·상시채용·마감일 없음** 각각의 fixture
4. 마감 신호를 어떻게 읽었는지 `references/boards.md`에 기록

🔴 **키를 새로 요구하는 보드는 신중히 봅니다.** "필수 키 1개"가 이 도구의 약속이고,
그 약속 때문에 이미 기능 하나(통근 시간 실측)를 보류하고 있습니다.

### 발견은 고쳤든 아니든 남깁니다

`docs/IMPROVEMENTS.md`가 이 저장소의 대장입니다. 결함을 찾았다면 고치기 전에 여기에 먼저 적어 주십시오.
**같은 실수가 다시 들어올 때 "왜 이렇게 돼 있는가"의 근거가 이 목록입니다.**

---

## 하지 않는 기여

- **자동 지원·이력서 자동 제출** — 계정 정지 위험이 사용자에게 옵니다
- **합격률·궁합 점수** — 검증할 수 없는 숫자는 만들지 않습니다
- **동의 없는 크롤링 확대** — 공개 API가 없는 보드는 열 때마다 사용자에게 묻습니다
- **npm 의존성** — 표준 라이브러리만 씁니다. CI에 설치 단계가 없어서 패키지를 들여오면 바로 깨집니다
